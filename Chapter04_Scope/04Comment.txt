4_1 지역변수, 범위, 지속기간

- 지역변수의 범위, 지속기간에 대한 이야기
- 블럭에 대한 개념
- 오버로딩에 대한 약간의 설명
- 네임스페이스에 관한 내용.

4_2 전역변수, 정적 변수, 내부 연결, 외부 연결

- 전역변수, 정적 변수, 내부 연결, 외부 연결에 대한 주의점 먼저 설명
- 정적 변수는 메모리가 정적이다. -> 초기화를 단 한번 한다. 대입을 반복하지 않는다. 그러므로 초기화는 선언시 무조건 해줘야한다.
- 전역변수의 위험성 설명 - 나중되면 관리가 힘들어진다. 그에 따른 대안도 몇가지 있다. 최소한으로 쓰는게 좋다.
- extern 키워드에 대한 설명 + 주의점

4_3 Using문과 모호성

- 모호성이란 같은 이름의 변수나 함수 등을 쓸 때 무엇을 써야할지 컴파일러에게 알려주는 것.
- using 문에 대한 설명
- header에 using문 쓰는건 좋진 않다.

4_4 auto 키워드

- auto에 대한 설명 초기화 하지않으면 사용할 수 없다.
- auto와 비슷한 템플릿에 대한 인지

4_5 형변환 Type conversion

- typeinfo 헤더에 대한 설명
- numeric promotion, numeric conversion에 대한 설명
- 자료형에 대한 우선순위 설명
- 암시적, 명시적 형 변환 설명

4_6 문자열 std string 소개

- 문자열에 대한 소개
- 버퍼에 대한 소개
- getline()함수 소개
- cin.ignore()함수 소개
- string 객체 소개

4_7 열거형 enumerated typeds

- 열거형 사용 이유, 사용법, 주의사항 설명

4_8 영역 제한 열거형(열거형 클래스)

- c++11 이후 적용. 

4_9 자료형에게 가명 붙여주기 Type aliases

- typedef문 설명 - 프로그램의 편의 유지관리에 좋다.
- 사용예제
- using문을 사용한 가명 지어주는 법

4_10 구조체 struct

- 구조체를 사용하는 이유
- 구조체 정의, 멤버에 대한 소개, '.' 에 대한 소개
- 멤버함수 소개 
- Data Padding에 대한 설명! 최적화에 대한 이야기
